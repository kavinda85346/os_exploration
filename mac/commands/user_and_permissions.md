### User Permissions

##### Given below are default commands in macOS which are used for user permission allocation.
    ```
    #Change the owner of a file/folder.
    sudo chown newuser /path/to/file_or_directory
    
    #Change the owner of a file/folder.
    sudo chown newuser /path/to/file_or_directory
    ```


