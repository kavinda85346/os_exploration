### Package Management

##### Given below are default commands in macOS which are used for package management.
    ```
    #Install packages.
    brew install git

    #Upgrade a package.
    brew upgrade git

    #Search packages.
    brew search git

    #List all the installed packages.
    brew list

    #Remove a package.
    brew uninstall vlc
    ```
    
